# MCP Tools — placeholder for future MCP integration
#
# Tools to implement:
# - get_schema(): Returns the database schema for allowed tables
# - run_sql_safe(sql, user_id): Validates and executes SQL with guardrails
# - get_document_summary(document_id): Returns a document summary
